torch>=2.2
transformers>=4.56   # dtype= (not torch_dtype=) requires 4.56.0+; verified via a real pip
                     # download+source-grep of 4.55.0 (no dtype= support) vs 4.56.0 (has it,
                     # torch_dtype= kept as a deprecated BC alias) before this bump
accelerate>=0.30
datasets>=2.19
scikit-learn>=1.4
scipy>=1.11
numpy>=1.26
pandas>=2.2
pyyaml>=6.0
matplotlib>=3.8
joblib>=1.4
huggingface_hub>=0.23
pytest>=8.0
nltk>=3.8   # scripts/run_fuzz.py self_bleu diversity metric (same precedent as scipy above)
bitsandbytes>=0.46.1  # scripts/judge.py loads the rubric-judge LLM in 8-bit (VRAM fix, judge
                      # only -- target models must always stay fp16, never quantized). Bumped
                      # from >=0.43 2026-08-06: a fresh Kaggle session's bootstrap install
                      # failed to bring in a working bitsandbytes below this floor.
