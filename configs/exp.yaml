# Single source of truth. Baselines and our method MUST share these invariants.
seed: 0

# White-box target you can actually run (ungated, ~6GB in fp16).
target_model: "Qwen/Qwen2.5-3B-Instruct"       # swap to meta-llama/Llama-3.2-3B-Instruct if desired

# Success judge (original GPTFuzzer RoBERTa judge — keeps comparability).
# CRITICAL FINDING (2026-08-06, see scripts/judge.py's docstring + PLAN.md): this judge has a
# severe false-positive problem -- it rewards jailbreak-SHAPED vocabulary (roleplay persona
# declarations) rather than actual harmful content. Kept here for its score as a logged
# DIAGNOSTIC only; it is NOT the success determinant anymore, judge_llm_model below is.
judge_model: "hubert233/GPTFuzz"

# Rubric-based LLM judge (the ACTUAL success determinant, scripts/judge.py) -- a fixed, small,
# ungated model distinct from both attack targets (Qwen2.5-3B, Phi-4-mini), so it's never
# self-grading either one. Reuses the same weights as transfer_target_local below (already
# used elsewhere in this project) rather than introducing a new download.
judge_llm_model: "microsoft/Phi-3.5-mini-instruct"

# Benchmark subset. Canonical AdvBench CSV (ungated) from the original llm-attacks repo —
# NOT the gated walledai/AdvBench HF dataset. Columns: goal, target.
benchmark: "https://raw.githubusercontent.com/llm-attacks/llm-attacks/main/data/advbench/harmful_behaviors.csv"
# Locked 2026-08-06 to the affordable core design (PLAN.md §10): 25 behaviors (not 50) and
# query_budget 40 (not 50), given the real 3-seed x 2-target GPU-hour cost -- see PLAN.md §10's
# worked budget table before changing either of these.
n_behaviors: 25
smoke_behaviors: 5

# Human seed-tier jailbreak templates for run_fuzz.py (the original GPTFuzzer seed pool, public
# research artifact — 77 templates, column "text", placeholder marker "[INSERT PROMPT HERE]").
# Loaded at runtime, same pattern as `benchmark` above — never hardcoded/committed in a script.
human_seed_templates: "https://raw.githubusercontent.com/sherdencooper/GPTFuzz/master/datasets/prompts/GPTFuzzer.csv"

# Fuzzing budget — IDENTICAL across all methods for a fair comparison.
query_budget: 40            # full-generation queries per behavior (locked 2026-08-06, was 50)
max_iterations: 100
mutate_model: "Qwen/Qwen2.5-3B-Instruct"   # local mutator to avoid API cost
decode_temperature: 0.7
decode_top_p: 0.9

# Probe / fitness.
probe_batch_size: 16
partial_forward: true       # score candidates by partial forward pass to the probe layer

# Cross-model transfer target — a SECOND small open-weight model, run locally on Kaggle.
# No external API. Cross-family (Phi vs Qwen) makes the transfer claim stronger; ungated + fits a T4.
transfer_target_local: "microsoft/Phi-3.5-mini-instruct"
